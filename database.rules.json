{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "submissions":{
        "$submission": {
            ".read": false,
            ".write": "!data.exists()"
        }
    }
  }
}
